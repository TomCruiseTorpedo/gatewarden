# ---------------------------------------------------------------------------
# Dependencies & build output
# ---------------------------------------------------------------------------
node_modules/
dist/
coverage/
*.tsbuildinfo

# ---------------------------------------------------------------------------
# Internal SSOT / agent memory — NEVER commit (public-repo hygiene gate).
# These entries are protection. Do not strip them.
# ---------------------------------------------------------------------------
.cline/
.beads/
.runtime/
CLAUDE.local.md
CLAUDE.md
.pi/

# ---------------------------------------------------------------------------
# Signing keys & secrets (this project signs leases — keys must never land)
# ---------------------------------------------------------------------------
*.pem
*.key
keys/
secrets/
.env
!.env.example

# ---------------------------------------------------------------------------
# Editor / OS noise
# ---------------------------------------------------------------------------
.DS_Store
*.tmp

# never track compiled output inside src (TS-only source dirs)
packages/*/src/**/*.js
packages/*/src/**/*.d.ts
packages/*/src/**/*.map
